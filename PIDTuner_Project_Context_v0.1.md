# PIDTuner 项目上下文与需求基线

> 用途：作为后续 AI Agent 进行需求细化、架构设计、任务拆分、代码实现与测试时的长期上下文。  
> 当前版本：v0.1  
> 项目暂定名：PIDTuner

---

## 1. 项目背景

工业设备中 PID 调节应用广泛，PID 参数的准确性会直接影响控制精度、响应速度、超调量、稳定性及设备运行质量。

当前常见调参方式存在以下问题：

- 工程师主要凭经验反复试调。
- 需要手工记录或导出设定值、实际值、控制输出等数据。
- 常借助 Excel、MATLAB 等外部工具进行二次分析。
- 数据采集、趋势查看、参数分析和参数修改之间相互割裂。
- 调试过程缺少统一记录，不利于复盘、比较和经验沉淀。

本项目希望将这些步骤整合为一个面向工业现场的 PID 调试辅助软件。

---

## 2. 项目目标

开发一个基于 C# 的 Windows 桌面程序，通过有线网络与 PLC 通信，实现：

1. 配置 PLC 连接信息。
2. 配置 PID 参数和相关过程量的 PLC 点位。
3. 实时读取、显示并记录 PID 运行数据。
4. 显示 SP、PV、MV 及 PID 参数的实时趋势曲线。
5. 对采集到的控制过程进行基础指标分析。
6. 给出 PID 参数调整建议。
7. 经工程师确认后，将新参数写回 PLC。
8. 将数据导出为 CSV，或持久化到本地数据库。
9. 保存每次试验、参数修改和分析结果，便于比较与复盘。

项目定位：

> 面向工业设备调试工程师的轻量化 PID 调试与数据分析工具。

---

## 3. 核心用户

主要用户：

- 工业设备调试工程师
- PLC 工程师
- 电气工程师
- 控制工程师
- 售后与现场服务工程师
- 测试设备研发人员

典型使用环境：

- 设备厂内调试
- 客户现场调试
- 汽车检测与试验设备
- 温度、压力、流量、转速等闭环控制场景

---

## 4. 典型业务流程

```text
创建调试项目
    ↓
配置 PLC 通信
    ↓
配置 SP / PV / MV / Kp / Ki(Ti) / Kd(Td) 点位
    ↓
连接 PLC
    ↓
开始实时采集
    ↓
观察趋势曲线
    ↓
触发或选取一段控制过程
    ↓
计算超调量、上升时间、调节时间、稳态误差等指标
    ↓
生成参数调整建议
    ↓
工程师确认
    ↓
写入 PLC
    ↓
再次试验并对比前后效果
    ↓
保存记录 / 导出 CSV / 生成报告
```

---

## 5. 功能需求

### 5.1 PLC 通信

程序应支持通过有线以太网连接 PLC。

首期目标：

- 优先支持西门子 PLC。
- 通信实现可选：
  - S7 协议库
  - PCHMI.dll
- 应通过统一接口封装通信层，避免业务代码依赖具体通信库。

建议接口：

```csharp
public interface IPlcClient
{
    Task ConnectAsync(CancellationToken cancellationToken);
    Task DisconnectAsync();
    Task<T> ReadAsync<T>(string address, CancellationToken cancellationToken);
    Task WriteAsync<T>(string address, T value, CancellationToken cancellationToken);
    bool IsConnected { get; }
}
```

通信层需要处理：

- 连接与断开
- 超时
- 自动重连
- 读取失败
- 写入失败
- 数据类型转换
- 批量读取
- 通信日志
- 连接状态展示

---

### 5.2 点位配置

用户能够配置以下点位：

必需点位：

- SP：设定值
- PV：实际值
- MV：控制输出

PID 参数点位：

- Kp
- Ki 或 Ti
- Kd 或 Td

可选点位：

- PID 自动/手动状态
- PID 启停状态
- 输出上下限
- 报警状态
- 设备运行状态
- 其他辅助过程量

每个点位建议包含：

```text
名称
PLC 地址
数据类型
读写权限
缩放系数
单位
描述
采样周期
是否启用
```

点位配置应支持保存、加载和复用。

---

### 5.3 实时数据采集

程序应按照设定采样周期持续读取 PLC 数据。

记录字段至少包括：

```text
时间戳
SP
PV
MV
Kp
Ki/Ti
Kd/Td
PLC连接状态
试验编号
参数方案编号
```

基本要求：

- 可开始、暂停、停止采集。
- 支持 100 ms、200 ms、500 ms、1 s 等采样周期。
- 采集任务不得阻塞 UI。
- 支持取消和异常恢复。
- 内存数据与数据库写入解耦。
- 数据量较大时应批量写入数据库。

---

### 5.4 实时趋势曲线

曲线至少显示：

- SP
- PV
- MV

可选显示：

- Kp
- Ki/Ti
- Kd/Td
- 误差 `SP - PV`
- 其他过程量

曲线功能：

- 实时刷新
- 暂停查看
- 缩放
- 平移
- 图例显示与隐藏
- 时间范围选择
- 游标查看具体值
- 不同曲线使用独立量程或双 Y 轴
- 从数据库加载历史曲线
- 标记参数修改时刻
- 对比两次或多次试验曲线

---

### 5.5 PID 过程分析

首期应支持以下基础指标：

- 最大值与最小值
- 峰值时间
- 超调量
- 上升时间
- 调节时间
- 稳态误差
- 平均绝对误差
- 均方误差
- 误差积分指标
- 输出波动程度
- 是否存在持续振荡
- 是否存在明显饱和

分析对象通常为一次设定值阶跃或用户手动选择的一段时间区间。

分析模块应与 UI、PLC 通信解耦，便于单独测试和扩展。

建议接口：

```csharp
public interface IPidAnalyzer
{
    PidAnalysisResult Analyze(
        IReadOnlyList<PidSample> samples,
        PidAnalysisOptions options);
}
```

---

### 5.6 PID 参数建议

首期建议以规则和经典方法为主，不直接以 AI 模型为核心。

可逐步支持：

1. 基于响应指标的经验规则。
2. Ziegler-Nichols 等经典整定方法。
3. 一阶惯性加纯滞后模型辨识。
4. 基于历史试验结果的参数对比。
5. 后续增加自动优化算法或机器学习方法。

输出内容：

- 当前参数
- 建议参数
- 调整方向
- 调整原因
- 预期影响
- 风险提示
- 建议可信度或适用条件

例如：

```text
Kp 建议降低 10%
原因：当前超调量过大且存在衰减振荡
预期：降低超调，但可能延长上升时间
```

---

### 5.7 参数写回 PLC

程序允许用户将建议参数写回 PLC，但必须满足：

- 默认禁止自动写回。
- 必须由工程师确认。
- 写入前显示旧值和新值。
- 支持参数范围校验。
- 支持写入权限控制。
- 写入后回读确认。
- 写入失败时明确提示。
- 保存完整操作日志。
- 支持恢复上一组参数。

不允许在无人确认的情况下自动持续修改现场 PLC 参数。

---

### 5.8 数据存储

首期推荐使用 SQLite。

建议保存以下内容：

- PLC 连接配置
- 点位配置
- 调试项目
- 试验记录
- 实时采样数据
- PID 参数方案
- 分析结果
- 参数修改记录
- 操作日志

建议核心实体：

```text
Project
PlcConnection
TagDefinition
TestSession
PidSample
PidParameterSet
PidAnalysisResult
ParameterChangeRecord
OperationLog
```

数据存储层应通过 Repository 或独立服务封装。

---

### 5.9 CSV 导入导出

支持：

- 将一次试验的原始采样数据导出 CSV。
- 导出分析指标。
- 导出参数修改记录。
- 从已有 CSV 导入离线数据进行分析。
- CSV 字段、单位和时间格式保持明确稳定。

导入离线数据后，即使未连接 PLC，也应能完成曲线查看和 PID 分析。

---

### 5.10 项目与试验管理

用户可以：

- 新建调试项目
- 保存 PLC 和点位配置
- 创建一次试验
- 填写设备、工况、备注
- 保存当前 PID 参数
- 对比不同试验
- 按日期、设备、参数方案查询历史记录

---

## 6. 界面需求

建议采用 WPF + MVVM。

主要页面：

### 6.1 连接与配置页

- PLC 类型
- IP 地址
- Rack / Slot 或其他连接参数
- 测试连接
- 点位配置表
- 数据类型和单位设置

### 6.2 实时监控页

- PLC 连接状态
- SP / PV / MV 实时值
- Kp / Ki(Ti) / Kd(Td)
- 实时趋势曲线
- 开始/暂停/停止采集
- 当前试验信息

### 6.3 分析页

- 选择分析区间
- 响应曲线
- 指标结果
- 问题判断
- 参数建议

### 6.4 参数调整页

- 当前参数
- 建议参数
- 手动编辑
- 参数范围校验
- 写回 PLC
- 回退上一组参数

### 6.5 历史记录页

- 试验列表
- 条件筛选
- 曲线回放
- 多试验对比
- CSV 导入导出

### 6.6 系统日志页

- 通信日志
- 采集日志
- 参数写入日志
- 异常日志

---

## 7. 建议技术栈

首期建议：

```text
语言：C#
运行时：.NET 8
桌面框架：WPF
架构：MVVM
MVVM 工具：CommunityToolkit.Mvvm
数据库：SQLite
数据访问：Dapper 或 EF Core
曲线：ScottPlot 或 LiveCharts2
日志：Serilog
配置：System.Text.Json
CSV：CsvHelper
PLC 通信：通过 IPlcClient 抽象，首期接入 S7 或 PCHMI
测试：xUnit
```

---

## 8. 建议软件架构

```text
PIDTuner
├── PIDTuner.Desktop
│   ├── Views
│   ├── ViewModels
│   └── UI Services
│
├── PIDTuner.Application
│   ├── UseCases
│   ├── DTOs
│   └── Interfaces
│
├── PIDTuner.Domain
│   ├── Models
│   ├── Analysis
│   └── Rules
│
├── PIDTuner.Infrastructure
│   ├── Plc
│   ├── Database
│   ├── Csv
│   ├── Logging
│   └── Configuration
│
└── PIDTuner.Tests
```

核心数据流：

```text
PLC
 ↓
IPlcClient
 ↓
数据采集服务
 ├──→ 实时状态缓存 → ViewModel → UI
 ├──→ 数据库
 └──→ PID 分析模块 → 参数建议
```

---

## 9. 非功能需求

### 稳定性

- 通信异常不能导致程序崩溃。
- PLC 断线后可自动重连。
- 数据库写入失败要记录并提示。
- 长时间运行不能出现明显内存持续增长。
- UI 线程不得执行阻塞式 PLC 通信。

### 可维护性

- PLC 通信、数据库、分析算法、UI 相互解耦。
- 不允许把主要业务逻辑写入窗口 code-behind。
- 关键模块具备单元测试。
- 配置和点位不得硬编码在界面逻辑中。

### 可扩展性

未来应能扩展：

- OPC UA
- Modbus TCP
- 不同品牌 PLC
- 更多 PID 算法
- 时序数据库
- Web 服务
- 远程监控
- 报告生成
- 多语言

### 安全性

- 参数写入必须确认。
- 对写入值进行上下限校验。
- 操作日志不可省略。
- 软件应明确区分“建议值”和“已生效值”。
- 首期不实现无人值守自动调参。

---

## 10. MVP 范围

首个可用版本只实现：

1. 西门子 PLC 连接。
2. SP、PV、MV、Kp、Ki/Ti、Kd/Td 点位配置。
3. 实时读取和显示。
4. 实时趋势曲线。
5. SQLite 数据记录。
6. CSV 导出。
7. 离线 CSV 导入。
8. 基础指标分析：
   - 超调量
   - 上升时间
   - 调节时间
   - 稳态误差
9. 手工输入新 PID 参数。
10. 人工确认后写回 PLC。
11. 保存参数修改记录。

MVP 暂不实现：

- 完全自动 PID 整定
- AI 模型训练
- 多用户协作
- 云端平台
- 多品牌 PLC
- 复杂权限系统
- 高级报告系统

---

## 11. 分阶段路线

### 阶段 1：离线分析原型

- 导入 CSV
- 显示曲线
- 选择分析区间
- 计算基础指标
- 输出分析结果

目的：先验证分析逻辑，不依赖 PLC。

### 阶段 2：实时采集

- 接入 PLC
- 点位配置
- 实时曲线
- SQLite 记录
- 通信异常处理

### 阶段 3：参数闭环

- 读取当前参数
- 生成调整建议
- 人工确认
- 写回 PLC
- 前后试验对比

### 阶段 4：增强分析

- 模型辨识
- 经典整定算法
- 多组参数对比
- 自动生成调试报告

---

## 12. 当前尚未确定的问题

后续需要继续明确：

1. 首期具体支持哪些西门子 PLC 型号。
2. 首期采用 S7 协议库还是 PCHMI.dll。
3. PLC 内 PID 参数的数据结构与地址格式。
4. 目标采样周期和最大点位数量。
5. 是否需要支持多个 PID 回路同时采集。
6. Ki/Kd 与 Ti/Td 的统一表示方式。
7. 参数写入时 PLC 是否需要切换手动模式或停止控制。
8. 分析算法针对哪些典型对象：
   - 温度
   - 压力
   - 流量
   - 转速
9. 是否需要生成 PDF/Excel 调试报告。
10. 是否需要支持用户自定义分析规则。

---

## 13. AI Agent 执行约束

AI Agent 在后续工作中应遵守：

1. 先澄清需求，再修改架构或代码。
2. 优先实现 MVP，不提前增加复杂功能。
3. PLC 通信必须通过接口抽象。
4. UI 不得直接访问 PLC 或数据库。
5. 分析算法必须可在离线数据上独立运行。
6. 参数写回必须保留人工确认步骤。
7. 每次实现应包含：
   - 变更说明
   - 关键设计决策
   - 测试方式
   - 已知限制
8. 不得在未说明的情况下修改公共接口或数据格式。
9. 对涉及工业现场安全的自动化功能保持保守。
10. 若需求与本文件冲突，应先指出冲突并请求确认。

---

## 14. 项目成功标准

MVP 成功的最低标准：

- 能稳定连接目标西门子 PLC。
- 用户可通过配置输入 PID 与过程量点位。
- 能连续记录 SP、PV、MV 和 PID 参数。
- 能实时显示趋势曲线。
- 能将一次试验保存到 SQLite 并导出 CSV。
- 能计算基础响应指标。
- 能保存并对比两组 PID 参数的试验结果。
- 能经人工确认安全地将参数写回 PLC。
- 通信断开、地址错误或数据库异常时程序不会崩溃。
