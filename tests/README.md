# Tests

Automated tests should be added when implementation starts. Initial priority:

- PID response metric calculations.
- CSV import/export field compatibility.
- Repository persistence behavior.
- PLC adapter error and reconnect behavior using fakes.
